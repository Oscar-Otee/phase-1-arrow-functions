// A function expression that returns the result of 2000 divide by 100.
const divide = function () {
    return 2000 / 100;
}

// An arrow function that returns the square of a number.
const square = x => x **2;
// An arrow function that adds two numbers and return the result.


const add = (a, b) => a + b;


console.log(add(23, 45));